name: Validate Prometheus

on:
  workflow_dispatch:
  pull_request:
    paths-ignore:
      - "prometheus/Dockerfile"
  merge_group:
    paths-ignore:
      - "prometheus/Dockerfile"

jobs:
  validate-prometheus:
    name: Validate Prometheus
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
